name: Deploy Project CI4

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.3'
        extensions: mbstring, xml, intl, curl, zip, gd
        tools: composer

    - name: Install Dependencies
      run: composer install --no-dev --optimize-autoloader

    # Step Tambahan: Inisialisasi SSH Agent secara manual
    - name: Install SSH Key
      run: |
        mkdir -p ~/.ssh
        # Simpan private key ke file
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
        chmod 600 ~/.ssh/id_ed25519
        
        # Jalankan ssh-agent dan langsung daftarkan env-nya ke GITHUB_ENV
        ssh-agent -a /tmp/ssh_agent.sock > /dev/null
        echo "SSH_AUTH_SOCK=/tmp/ssh_agent.sock" >> $GITHUB_ENV
        
        # Tambahkan key ke agent menggunakan socket yang baru dibuat
        SSH_AUTH_SOCK=/tmp/ssh_agent.sock ssh-add ~/.ssh/id_ed25519
        
        # Tambahkan known_hosts agar tidak ditanya fingerprint lagi
        echo "${{ secrets.SSH_KNOWN_HOSTS }}" > ~/.ssh/known_hosts
        chmod 644 ~/.ssh/known_hosts

    - name: Deploy with Deployer
      uses: deployphp/action@v1
      with:
        # Kita tetap masukkan ini sebagai fallback
        private-key: ${{ secrets.SSH_PRIVATE_KEY }}
        known-hosts: ${{ secrets.SSH_KNOWN_HOSTS }}
        # Pastikan "production" tertulis eksplisit
        dep: deploy production -vvv